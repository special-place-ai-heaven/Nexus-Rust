name: Rust

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Select pinned Rust toolchain
        run: rustup show

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Run tests
        run: cargo test --workspace --all-targets --all-features

      - name: Run strict Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Build documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --workspace --no-deps

      - name: Build release proxy
        run: cargo build -p nexus-runtime --release

      - name: Verify proxy exports
        run: cargo run -p xtask --release -- verify-exports target/release/d3d11.dll

      # The proxy must load without a Visual C++ redistributable. A build that fails to
      # pick up the crt-static rustflags is dynamically linked with no other outward sign,
      # so this gate is what makes the fix stick.
      - name: Verify static C runtime and import surface
        run: cargo run -p xtask --release -- verify-imports target/release/d3d11.dll

      # Third-party addon managers identify the installed host by FileVersion. An
      # unversioned DLL reads as unknown and can be overwritten, undoing the swap.
      - name: Verify embedded version resource
        run: cargo run -p xtask --release -- verify-version target/release/d3d11.dll

      - name: Smoke-test system forwarding
        run: cargo run -p xtask --release -- smoke-proxy target/release/d3d11.dll

      - name: Archive proxy
        uses: actions/upload-artifact@v4
        with:
          name: d3d11-rust
          path: |
            target/release/d3d11.dll
            target/release/d3d11.pdb
          if-no-files-found: error
